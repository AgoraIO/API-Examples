{
  "name": "Build After Task Complete",
  "version": "1.0.0",
  "description": "Automatically runs assembleDebug after a Spec task completes to verify the build",
  "when": {
    "type": "postTaskExecution"
  },
  "then": {
    "type": "runCommand",
    "command": "cd APIExample-Audio && ./gradlew assembleDebug --stacktrace 2>&1 | tail -20"
  }
}
