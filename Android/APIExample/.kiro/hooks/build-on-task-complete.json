{
  "name": "Build After Task Complete",
  "version": "1.0.0",
  "description": "Automatically runs assembleDebug to verify compilation after a spec task completes",
  "when": {
    "type": "postTaskExecution"
  },
  "then": {
    "type": "runCommand",
    "command": "cd APIExample && ./gradlew assembleDebug --stacktrace 2>&1 | tail -20"
  }
}
