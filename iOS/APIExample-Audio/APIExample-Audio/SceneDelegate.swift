import UIKit

@available(iOS 13.0, *)
class SceneDelegate: UIResponder, UIWindowSceneDelegate {
    // UIKit creates this window from Main.storyboard in the scene configuration.
    var window: UIWindow?
}
